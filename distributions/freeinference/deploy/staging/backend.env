# Read only by ops/deploy/deploy_staging.sh, after the shared files above
# and before the server's .env. Anything staging must not inherit from
# production belongs here.

# Without this, staging's label came from finding "staging" in its host
# name, which is true today and silent about it.
DEPLOYMENT_ENV=staging
