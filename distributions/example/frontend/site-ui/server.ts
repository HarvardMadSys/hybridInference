/**
 * Static half of the example module.
 *
 * Both fields are optional, and this module sets neither: the console's own
 * document language and title are the correct ones for a deployment whose only
 * change is its home page. The file exists because the interface documents a
 * `server` entry and a module without one is a module that forgot it, not one
 * that declined.
 */
export {};
