# Transient pipeline state (sentinels) — regenerated by the orchestrator
results/.state/

# genai-perf raw artifact subdirs: large synthetic-input payloads (inputs.json)
# and per-run CSVs. The canonical per-point JSON (results/<engine>/*.json) and
# the aggregated summary.csv + plots are kept; these working dirs are not.
results/*/MiniMaxAI_*-openai-completions-concurrency*/

# Python bytecode
__pycache__/
*.pyc
