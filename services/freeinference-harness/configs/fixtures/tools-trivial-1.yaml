# Trivial single-tool fixture -- backwards compat with original forced_tool_call scenario.
tools:
  - type: function
    function:
      name: record_findings
      description: Record a short finding for regression testing.
      parameters:
        type: object
        properties:
          summary:
            type: string
        required:
          - summary
