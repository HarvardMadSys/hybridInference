{
  "name": "alert-control-plane-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Dormant Cloudflare Durable Object control plane for FreeInference incidents.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --config wrangler.example.toml",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250620.0",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0",
    "wrangler": "^3.80.0"
  }
}
