# Example models registry for the OpenRouter-compatible server.
# Values like ${ENV_VAR} will be expanded from environment variables.

models:
  - id: llama-3.3-70b-instruct
    name: Llama 3.3 70B Instruct
    provider: llama
    provider_model_id: "Llama-3.3-70B-Instruct"
    base_url: ${LLAMA_BASE_URL}
    api_key: ${LLAMA_API_KEY}
    quantization: "bf16"
    input_modalities: ["text"]
    output_modalities: ["text"]
    context_length: 131072
    max_output_length: 8192
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, top_k, min_p, max_tokens, stop, seed]
    aliases: ["llama-3.3-70b-instruct"]
    pricing:
      prompt: "0"
      completion: "0"
      image: "0"
      request: "0"
      input_cache_reads: "0"
      input_cache_writes: "0"
    route:
      # Local VLLM endpoint (if available)
      - kind: vllm
        weight: 1.0
        base_url: ${LOCAL_BASE_URL}
        provider_model_id: "/models/llama-3.3-70b"
      # Remote Llama API endpoint
      - kind: llama
        weight: 1.0
        base_url: ${LLAMA_BASE_URL}
        api_key: ${LLAMA_API_KEY}

  - id: llama-4-scout
    name: Llama 4 Scout
    provider: llama
    provider_model_id: "Llama-4-Scout-17B-16E-Instruct-FP8"
    base_url: ${LLAMA_BASE_URL}
    api_key: ${LLAMA_API_KEY}
    quantization: "fp8"
    input_modalities: ["text"]
    output_modalities: ["text"]
    context_length: 128000
    max_output_length: 16384
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, top_k, min_p, max_tokens, stop, seed]
    aliases: ["/models/meta-llama_Llama-4-Scout-17B-16E"]
    pricing:
      prompt: "0"
      completion: "0"
      image: "0"
      request: "0"
      input_cache_reads: "0"
      input_cache_writes: "0"
    route:
      # Remote Llama API endpoint
      - kind: llama
        weight: 1.0
        base_url: ${LLAMA_BASE_URL}
        api_key: ${LLAMA_API_KEY}

  - id: llama-4-maverick
    name: Llama 4 Maverick
    provider: llama
    provider_model_id: "Llama-4-Maverick-17B-128E-Instruct-FP8"
    base_url: ${LLAMA_BASE_URL}
    api_key: ${LLAMA_API_KEY}
    quantization: "fp8"
    input_modalities: ["text", "image"]
    output_modalities: ["text"]
    context_length: 128000
    max_output_length: 16384
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, top_k, min_p, max_tokens, stop, seed]
    aliases: ["/models/meta-llama_Llama-4-Maverick-17B-128E"]
    pricing:
      prompt: "0"
      completion: "0"
      image: "0"
      request: "0"
      input_cache_reads: "0"
      input_cache_writes: "0"
    route:
      - kind: llama
        weight: 1.0
        base_url: ${LLAMA_BASE_URL}
        api_key: ${LLAMA_API_KEY}

  # - id: deepseek-chat
  #   name: DeepSeek Chat
  #   provider: deepseek
  #   base_url: https://api.deepseek.com/v1
  #   api_key: ${DEEPSEEK_API_KEY}
  #   quantization: "bf16"
  #   input_modalities: ["text"]
  #   output_modalities: ["text"]
  #   context_length: 65536
  #   max_output_length: 8192
  #   supports_tools: true
  #   supports_structured_output: true
  #   supported_params: [temperature, top_p, max_tokens, stop, frequency_penalty, presence_penalty]
  #   pricing:
  #     prompt: "0.28"  # Input tokens (cache miss) per 1M tokens
  #     completion: "0.42"  # Output tokens per 1M tokens
  #     image: "0"
  #     request: "0"
  #     input_cache_reads: "0.028"  # Cache hit tokens per 1M tokens (90% discount)
  #     input_cache_writes: "0"  # No separate write cost
  #   route:
  #     - kind: deepseek
  #       weight: 1.0
  #       base_url: https://api.deepseek.com/v1
  #       api_key: ${DEEPSEEK_API_KEY}

  - id: gemini-2.5-flash
    name: Gemini 2.5 Flash
    provider: gemini
    provider_model_id: "gemini-2.5-flash"
    base_url: https://generativelanguage.googleapis.com/v1beta
    api_key: ${GEMINI_API_KEY}
    quantization: "bf16"
    input_modalities: ["text", "image"]
    output_modalities: ["text"]
    context_length: 1048576
    max_output_length: 65536
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, top_k, max_tokens, stop]
    pricing:
      prompt: "0.15"  # Input tokens per 1M tokens
      completion: "1.25"  # Output tokens (including thinking) per 1M tokens
      image: "0"
      request: "0"
      input_cache_reads: "0.075"  # Cache hit tokens per 1M tokens (50% discount)
      input_cache_writes: "1.0"  # Cache storage per 1M tokens per hour
    route:
      - kind: gemini
        weight: 1.0
        base_url: https://generativelanguage.googleapis.com/v1beta
        api_key: ${GEMINI_API_KEY}
        provider_model_id: "gemini-2.5-flash"

  - id: gemini-2.5-flash-preview-09-2025
    name: Gemini 2.5 Flash Preview (Sep 2025)
    provider: gemini
    provider_model_id: "gemini-2.5-flash-preview-09-2025"
    base_url: https://generativelanguage.googleapis.com/v1beta
    api_key: ${GEMINI_API_KEY}
    quantization: "bf16"
    input_modalities: ["text", "image"]
    output_modalities: ["text"]
    context_length: 1048576
    max_output_length: 65536
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, top_k, max_tokens, stop]
    pricing:
      prompt: "0.15"  # Input tokens per 1M tokens (same as stable)
      completion: "1.25"  # Output tokens (including thinking) per 1M tokens
      image: "0"
      request: "0"
      input_cache_reads: "0.075"  # Cache hit tokens per 1M tokens (50% discount)
      input_cache_writes: "1.0"  # Cache storage per 1M tokens per hour
    route:
      - kind: gemini
        weight: 1.0
        base_url: https://generativelanguage.googleapis.com/v1beta
        api_key: ${GEMINI_API_KEY}
        provider_model_id: "gemini-2.5-flash-preview-09-2025"

  - id: gemini-1.5-flash
    name: Gemini 1.5 Flash
    provider: gemini
    provider_model_id: "gemini-1.5-flash"
    base_url: https://generativelanguage.googleapis.com/v1beta
    api_key: ${GEMINI_API_KEY}
    quantization: "bf16"
    input_modalities: ["text", "image"]
    output_modalities: ["text"]
    context_length: 1048576
    max_output_length: 8192
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, top_k, max_tokens, stop]
    pricing:
      prompt: "0.075"  # Input tokens per 1M tokens
      completion: "0.30"  # Output tokens per 1M tokens
      image: "0"
      request: "0"
      input_cache_reads: "0.01875"  # Cache hit tokens per 1M tokens (75% discount)
      input_cache_writes: "1.0"  # Cache storage per 1M tokens per hour
    route:
      - kind: gemini
        weight: 1.0
        base_url: https://generativelanguage.googleapis.com/v1beta
        api_key: ${GEMINI_API_KEY}
        provider_model_id: "gemini-1.5-flash"

  - id: gemini-1.5-flash-8b
    name: Gemini 1.5 Flash-8B
    provider: gemini
    provider_model_id: "gemini-1.5-flash-8b"
    base_url: https://generativelanguage.googleapis.com/v1beta
    api_key: ${GEMINI_API_KEY}
    quantization: "bf16"
    input_modalities: ["text", "image"]
    output_modalities: ["text"]
    context_length: 1048576
    max_output_length: 8192
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, top_k, max_tokens, stop]
    aliases: ["gemini-flash-lite"]
    pricing:
      prompt: "0.0375"  # Input tokens per 1M tokens (50% of 1.5 Flash)
      completion: "0.15"  # Output tokens per 1M tokens (50% of 1.5 Flash)
      image: "0"
      request: "0"
      input_cache_reads: "0.009375"  # Cache hit tokens per 1M tokens (75% discount)
      input_cache_writes: "1.0"  # Cache storage per 1M tokens per hour
    route:
      - kind: gemini
        weight: 1.0
        base_url: https://generativelanguage.googleapis.com/v1beta
        api_key: ${GEMINI_API_KEY}
        provider_model_id: "gemini-1.5-flash-8b"

  - id: gemini-1.5-pro
    name: Gemini 1.5 Pro
    provider: gemini
    provider_model_id: "gemini-1.5-pro"
    base_url: https://generativelanguage.googleapis.com/v1beta
    api_key: ${GEMINI_API_KEY}
    quantization: "bf16"
    input_modalities: ["text", "image"]
    output_modalities: ["text"]
    context_length: 2097152
    max_output_length: 8192
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, top_k, max_tokens, stop]
    pricing:
      prompt: "1.25"  # Input tokens per 1M tokens
      completion: "5.00"  # Output tokens per 1M tokens
      image: "0"
      request: "0"
      input_cache_reads: "0.3125"  # Cache hit tokens per 1M tokens (75% discount)
      input_cache_writes: "4.50"  # Cache storage per 1M tokens per hour
    route:
      - kind: gemini
        weight: 1.0
        base_url: https://generativelanguage.googleapis.com/v1beta
        api_key: ${GEMINI_API_KEY}
        provider_model_id: "gemini-1.5-pro"

  - id: gemini-2.0-flash-exp
    name: Gemini 2.0 Flash Experimental
    provider: gemini
    provider_model_id: "gemini-2.0-flash-exp"
    base_url: https://generativelanguage.googleapis.com/v1beta
    api_key: ${GEMINI_API_KEY}
    quantization: "bf16"
    input_modalities: ["text", "image"]
    output_modalities: ["text"]
    context_length: 1048576
    max_output_length: 8192
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, top_k, max_tokens, stop]
    pricing:
      prompt: "0"  # Free during experimental phase
      completion: "0"  # Free during experimental phase
      image: "0"
      request: "0"
      input_cache_reads: "0"
      input_cache_writes: "0"
    route:
      - kind: gemini
        weight: 1.0
        base_url: https://generativelanguage.googleapis.com/v1beta
        api_key: ${GEMINI_API_KEY}
        provider_model_id: "gemini-2.0-flash-exp"

  # Zhipu AI models (OpenAI-compatible)
  - id: glm-4.5
    name: GLM-4.5
    provider: zhipu
    quantization: "fp16"
    context_length: 128000
    max_output_length: 96000
    supports_tools: true
    supports_structured_output: true
    supported_params: [temperature, top_p, max_tokens, stop, stream]
    input_modalities: ["text"]
    output_modalities: ["text"]
    pricing:
      prompt: "0.2"  # Input tokens per 1M tokens
      completion: "1.1"  # Output tokens per 1M tokens
      image: "0"
      request: "0"
      input_cache_reads: "0.03"  # Cache hit tokens per 1M tokens (85% discount)
      input_cache_writes: "0"  # Cache storage is limited time free
    route:
      - kind: zhipu  # Using dedicated Zhipu adapter
        weight: 1.0
        base_url: https://open.bigmodel.cn/api/paas/v4
        api_key: ${ZAI_API_KEY}
        provider_model_id: "glm-4.5"
