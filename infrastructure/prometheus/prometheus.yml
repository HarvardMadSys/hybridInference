global:
  # Default for local/dev; consider 30s in production to reduce overhead.
  scrape_interval: 15s
  evaluation_interval: 30s
  external_labels:
    source: internal

scrape_configs:
  - job_name: app
    metrics_path: /metrics
    static_configs:
      - targets: ["backend:8080"]
    relabel_configs: []

  # Example: blackbox probe (requires blackbox-exporter running)
  # - job_name: blackbox
  #   metrics_path: /probe
  #   params:
  #     module: [http_2xx]
  #   static_configs:
  #     - targets:
  #       - http://localhost:8000/health
  #       - http://localhost:8000/metrics
  #   relabel_configs:
  #     - source_labels: [__address__]
  #       target_label: __param_target
  #     - source_labels: [__param_target]
  #       target_label: instance
  #     - target_label: __address__
  #       replacement: 127.0.0.1:9115 # blackbox-exporter

rule_files: []
# Note: previously referenced rules/service_availability.yml which was
# removed in commit ec0dec4 ("remove prometheus + grafana, keep
# alertmanager + alert-logger"). The rules depended on `up{job="app"}`
# (no scrape job is configured for the app — see scrape_configs above —
# so `up` would always be 0 if even reachable) and the
# `database_connected` gauge (the `prometheus-client` library was
# dropped and the gauge is now a no-op shim). Re-add rule files here
# only after a Prometheus metrics endpoint is wired back in.

# Alertmanager configuration
alerting:
  alertmanagers:
    - static_configs:
        - targets: ["alertmanager:9093"]  # Alertmanager address
