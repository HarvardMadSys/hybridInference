# --- All rules commented out (2026-02-11) ---
# ProviderAvailabilityLow: provider_availability metric is not reliably implemented.
# #1 noise source — 265h continuous firing (2026-01-28 to 2026-02-11).
# Re-enable when provider_availability metric is fixed.

groups:
  - name: pipeline-health
    interval: 30s
    rules: []
    # - alert: ProviderAvailabilityLow
    #   expr: provider_availability < 0.90
    #   for: 10m
    #   labels:
    #     severity: watch
    #   annotations:
    #     summary: "Provider {{ $labels.provider }} availability < 90%"
    #     description: "Provider {{ $labels.provider }} reports sustained availability degradation (current: {{ $value | humanizePercentage }})."
