global:
  scrape_interval: 30s
  evaluation_interval: 30s

rule_files:
  - "rules/external_probe.yml"
  - "rules/latency_probe.yml"

alerting:
  alertmanagers:
    - static_configs:
        - targets: ["127.0.0.1:9093"]

scrape_configs:
  - job_name: llm_latency_prober
    scrape_interval: 5m
    static_configs:
      - targets: ["127.0.0.1:9116"]

  - job_name: blackbox
    metrics_path: /probe
    params:
      module: [http_2xx]
    static_configs:
      - targets:
          - https://freeinference.org/v1/models
    relabel_configs:
      - source_labels: [__address__]
        target_label: __param_target
      - source_labels: [__param_target]
        target_label: instance
      - target_label: __address__
        replacement: 127.0.0.1:9115  # blackbox exporter address
