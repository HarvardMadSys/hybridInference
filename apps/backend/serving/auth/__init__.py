"""Authentication and signup policy helpers.

This package hosts feature-specific auth helpers that don't belong in
``serving.servers.auth`` (HTTP-routing helpers) or ``serving.utils``
(general-purpose utilities). Currently only the signup domain allowlist
policy lives here.
"""
